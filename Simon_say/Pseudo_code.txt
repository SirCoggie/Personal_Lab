#create a connection between the start button, click me button (1, 2, 3, 4), score button, instruction

#let all variables exist
#variables such as score,highscore,GameState,ButtonColor

#function TimeRandom(min,max):
    # random number * (max-min) + min;

#for each button in clicker addEventListener for click and handleClickerClick

#function clearbuttons
    #for each button in clicker remove active

#def buttonColorFunc
    #toggle clicker class as active on and off

#def startColorLoop
    #Create TimeRandom
    #exectures setTimeout (execture a function asfter time delay)
        - Execute this function
            - if not in game state, return
            - ButtonColorFunc(toggle clicker class on/off)
            - executes startcolor loop again
        - every TimeRandom

#def handleStartClick
    #if GameState === false:
        #GameState = True
        #reset everything
        #remove active from clicker if any
        #instruction sheet say press the button when its red
        #startColorLoop executed

    #if GameState === true:
        #remind player that its on
        #instruction sheet say "follow instruction"

#def handleClickerClick
    #if GameState === true:
        #add score
        #display score
        #instruction say good job
        
        #else:
        #say you lost
        #if score > highscore:
            #replace
        #game state = false
        #reset game
        #clear timeoutID
        #display "Game over! Press start to play again"
